version: '3.9'
services:
    music:
        build: 
            context: .
            dockerfile: Music/Dockerfile 
        restart: always

    manage:
        build:
            context: .
            dockerfile: Manage/Dockerfile
        restart: always